%h1 Books

= form_tag search_books_path, :method => :get do
  = text_field_tag :query, nil, placeholder: "Title/Author name"
  = select_tag :category, options_for_select(@categories)
  = submit_tag "Search"

%ol
  = render @books
= will_paginate @books