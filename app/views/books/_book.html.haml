%li
  .book-item
    = link_to image_tag(book.photo.url(:thumb)), book_path(book)
    .book-item-title
      = link_to book.title, book_path(book)
    .book-item-author
      = "By: #{book.author_name}"